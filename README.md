# vix
vim environment

## install
clone it to `~/.vix`

``` shell
git clone git@github.com:eryx/vix.git ~/.vix
```

## run
1. vim -u ~/.vix/vimrc
2. add `alias vix="vim -u ~/.vix/vimrc"` to `~/.bashrc`






